import mensajeros.*
import destinos.*


describe "group of tests for pepita" {

  test "roberto puede llevar un paquete en camion con acoplado" {
    camion.setAcoplados(1)
    assert.that(brooklin.pasar(chuck))
  }

}